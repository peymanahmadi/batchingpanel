import { ProfileOptions, ProfileContainer } from "../../components/shared";

const Profile = () => {
  return (
    <>
      <ProfileOptions />
      <ProfileContainer />
    </>
  );
};

export default Profile;
