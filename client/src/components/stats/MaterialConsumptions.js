// import { useEffect } from "react";
// import { useAppContext } from "../../context/appContext";

const MaterialConsumptions = () => {
  // const { getMaterialConsumption, matconsume } = useAppContext();

  // const condition = {
  //   customerCodeName: "goldasht",
  //   dueDate: "2022-09-03",
  // };

  // useEffect(() => {
  //   getMaterialConsumption(condition);
  // }, []);

  return (
    <section>
      {/* {matconsume.map((mat, index) => {
        return <h1 key={index}>1</h1>;
      })} */}
    </section>
  );
};

export default MaterialConsumptions;
